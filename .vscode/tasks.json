{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "cppbuild",
            "label": "C/C++: g++.exe build active file with SDL (Release)",
            "command": "C:\\msys64\\ucrt64\\bin\\g++.exe",
            "args": [
                "-fdiagnostics-color=always",
                "-O3",  // Aggressive optimizations
                "-ffast-math",
//                "-flto",  // Link-Time Optimization
                "-DNDEBUG",  // Disable asserts for release mode
                "-s",  // Strip debugging information
                "-I", "C:/msys64/mingw64/include",
                "-I", "${workspaceFolder}/src/Helper",
                "-I", "${workspaceFolder}/src/Math",
                "-I", "${workspaceFolder}/src/Object",
                "-I", "${workspaceFolder}/src/Rendering",
                "-I", "${workspaceFolder}/src/Scenes",
                "-I", "${workspaceFolder}/src/Textures",
                "${workspaceFolder}/*.cpp",
                "${workspaceFolder}/src/Helper/*.cpp",
                "${workspaceFolder}/src/Math/*.cpp",
                "${workspaceFolder}/src/Object/*.cpp",
                "${workspaceFolder}/src/Rendering/*.cpp",
                "${workspaceFolder}/src/Scenes/*.cpp",
                "-L", "C:/msys64/mingw64/lib",
                "-lmingw32",
                "-finline-functions",  // Enable aggressive function inlining
                "-lopengl32",
                "-lgdi32",
                "-lSDL2main",
                "-lSDL2",  // Link against SDL2
                "-lwinmm",
                "-lSDL2_image",
                "-o",
//                "-fprofile-generate",  // Profiling build
//                "-fprofile-use",  // Use the generated profile for final optimizations
                "${fileDirname}\\Rasterization_renderer.exe"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": ["$gcc"],
            "group": "build",
            "detail": "Build with SDL(Release)"
        },
    ]
}
